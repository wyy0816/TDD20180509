const Fbnq=require('../lib/Fbnq')
describe("Fbnq", ()=> {

    it("should return 1 when input 1", ()=> {
        expect(Fbnq(1)).toBe(1)
    })
    it("should return 1 when input 2", ()=> {
        expect(Fbnq(2)).toBe(1)
    })
    
})